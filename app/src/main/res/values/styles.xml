<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!--by wenz11-->
    <style name="AppTheme.Splash">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <!--by wenz11 to validate Login Credentials-->
    <style name="error" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/colorCancelRipple</item>
        <item name="android:textSize">12sp</item>
    </style>

    <!--by wenz11 to validate Login Credentials-->
    <style name="errorForPassword" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/colorCancelRipple</item>
        <item name="android:textSize">12sp</item>
    </style>

</resources>
